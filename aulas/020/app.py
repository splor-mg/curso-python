# métodos de listas 
# append = adc itens
# insert = adc itens na posição index
# remove = remove um item
# clear = limpa a lista
# pop = exclui o último item
# index = se o número existe
# count = quantas vezes um valor aparece
# sort = ordenar a lista em ordem crescente 
# reverse = inverte a ordem dos itens a lista 
# copy = copia a lista

numeros = [7, 5, 8, 2, 0, 8]
numeros2 = numeros.copy()
numeros.append(1)
print(numeros2)
